// Animation features (incl. drag for swiping photos and layout for the nav dot and filter pill).
// Loaded after the page renders; see LazyMotion in App.tsx.
import { domMax } from 'framer-motion'

export default domMax
